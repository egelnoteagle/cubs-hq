# assets

- **`cubs_logo_source.png`** — *you provide this.* A high-resolution Cubs logo
  (transparent PNG preferred). Committed to the repo.
- **`w_flag_64x64.png`** — generated by `prepare_assets.py` (gitignored).
- **`cubs_logo_64x64.png`** — generated by `prepare_assets.py` from the source
  above (gitignored).

Regenerate the two 64×64 assets at any time:

```bash
python3 prepare_assets.py
```
